import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.solardashboard.mobile',
  appName: 'Solar Dashboard Mobile',
  webDir: 'out',
  server: {
    hostname: 'localhost'
  }
};

export default config;
